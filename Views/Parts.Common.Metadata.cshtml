@{

//var date = Model.ContentPart.PublishedUtc.Date;
var date = Model.ContentPart.CreatedUtc.Date;

}

<div class="published">
    <span class="month">
        @date.ToString("MMM")
    </span>
    <span class="day">
        @date.ToString("dd")
    </span>
    <span class="year">
        @date.ToString("yyyy")
    </span>
</div>
